{
  "name": "hero-awesome-pro",
  "version": "1.0.3",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc && cp package.json README.md ./dist"
  },
  "author": "Hero",
  "license": "ISC",
  "dependencies": {
    "react": "^18.1.0"
  },
  "devDependencies": {
    "@types/react": "^18.0.12",
    "react-dom": "^18.1.0",
    "typescript": "^4.7.3"
  }
}
